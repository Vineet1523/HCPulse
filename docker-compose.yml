services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: hcpulse
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - hcpulse_data:/var/lib/postgresql/data

volumes:
  hcpulse_data:
